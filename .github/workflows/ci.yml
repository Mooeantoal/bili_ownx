name: CI/CD

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: write
  actions: read

jobs:
  standard-build:
    name: Standard Build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      TARGET_SDK_VERSION: '36'
      FALLBACK_SDK_VERSION: '35'
      BUILD_TOOLS_VERSION: '36.0.0'
      FALLBACK_BUILD_TOOLS_VERSION: '35.0.0'
      JAVA_VERSION: '17'
      # Dynamic variables set during the job
      ACTUAL_PLATFORM_VERSION: ''
      COMPILE_SDK_VERSION: ''
      ACTUAL_BUILD_TOOLS_VERSION: ''
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 设置 Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'gradle'
          cache-dependency-path: |
            android/build.gradle
            android/app/build.gradle.kts

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          log-accepted-android-sdk-licenses: false

      - name: Install Android SDK Packages
        timeout-minutes: 10
        run: |
          echo "🚀 Installing Android SDK components..."
          
          # Install Build Tools and Platforms
          yes | sdkmanager --licenses >/dev/null 2>&1
          sdkmanager "platforms;android-$TARGET_SDK_VERSION" "build-tools;$BUILD_TOOLS_VERSION" "platform-tools"
          
          # Verify Installation
          echo "🔍 Verifying SDK installation..."
          ls -la $ANDROID_HOME/platforms/
          if [ -d "$ANDROID_HOME/platforms/android-$TARGET_SDK_VERSION" ]; then
             echo "✅ Android $TARGET_SDK_VERSION installed successfully"
             echo "COMPILE_SDK_VERSION=$TARGET_SDK_VERSION" >> $GITHUB_ENV
             echo "ACTUAL_PLATFORM_VERSION=android-$TARGET_SDK_VERSION" >> $GITHUB_ENV
             echo "ACTUAL_BUILD_TOOLS_VERSION=$BUILD_TOOLS_VERSION" >> $GITHUB_ENV
          else
             echo "❌ Failed to install Android $TARGET_SDK_VERSION"
             exit 1
          fi

      - name: 配置构建环境
        timeout-minutes: 5
        run: |
          set -e
          echo "🔒 配置 Gradle 和 Flutter 环境..."
          
          # 验证环境变量
          if [ -z "$ANDROID_HOME" ]; then
            echo "❌ ANDROID_HOME 未设置"
            exit 1
          fi
          
          # 禁止 Gradle 自动下载 SDK
          mkdir -p android
          echo "android.builder.sdkDownload=false" >> android/gradle.properties
          echo "org.gradle.daemon=false" >> android/gradle.properties
          echo "org.gradle.configureondemand=true" >> android/gradle.properties
          
          # 生成 local.properties
          cat > android/local.properties << 'EOF'
          sdk.dir=$ANDROID_HOME
          flutter.sdk=\$FLUTTER_ROOT
          flutter.buildMode=debug
          flutter.versionName=1.0.0
          flutter.versionCode=1
          EOF
          
          # 设置 Flutter 环境变量
          echo "FLUTTER_ANDROID_SDK_ROOT=$ANDROID_HOME" >> $GITHUB_ENV
          echo "GRADLE_USER_HOME=$PWD/.gradle" >> $GITHUB_ENV
          # echo "FLUTTER_ROOT=/opt/hostedtoolcache/flutter/stable-3.27.5-x64" >> $GITHUB_ENV
          
          echo "✅ 构建环境配置完成"

      - name: 缓存 Gradle 依赖
        uses: actions/cache@v4
        timeout-minutes: 5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            .gradle
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: 设置 Flutter
        uses: subosito/flutter-action@v2.16.0
        timeout-minutes: 10
        with:
          flutter-version: '3.27.x'
          channel: 'stable'
          cache: true
          cache-key: "flutter-${{ runner.os }}-${{ hashFiles('**/pubspec.yaml', '**/pubspec.lock') }}"

      - name: 构建 APK (Debug)
        timeout-minutes: 20
        run: |
          set -e
          echo "🚀 开始构建 APK..."
          
          # 验证环境变量
          if [ -z "$COMPILE_SDK_VERSION" ]; then
            echo "❌ COMPILE_SDK_VERSION 未设置"
            exit 1
          fi
          
          if [ -z "$ACTUAL_PLATFORM_VERSION" ]; then
            echo "❌ ACTUAL_PLATFORM_VERSION 未设置"
            exit 1
          fi
          
          echo "使用的平台版本: $ACTUAL_PLATFORM_VERSION"
          echo "编译 SDK 版本: $COMPILE_SDK_VERSION"
          
          # 清理并准备
          flutter clean
          flutter pub cache clean
          flutter pub get
          flutter doctor -v
          
          # 如果需要降级版本，使用 sed 高效更新配置
          # 执行构建
          flutter build apk --debug || {
            echo "❌ 构建失败，开始诊断..."
            flutter doctor -v
            echo "Gradle 配置:"
            cat android/local.properties
            exit 1
          }

      - name: 验证构建结果
        timeout-minutes: 5
        run: |
          set -e
          echo "🔍 查找并验证生成的 APK 文件..."
          
          # 查找APK文件
          APK_FILES=$(find . -name "*.apk" -type f 2>/dev/null || true)
          
          if [ -z "$APK_FILES" ]; then
            echo "❌ 未找到 APK 文件"
            exit 1
          fi
          
          echo "✅ 找到以下 APK 文件:"
          for apk in $APK_FILES; do
            if [ -f "$apk" ]; then
              SIZE=$(stat -c%s "$apk")
              echo "- $(basename "$apk") ($SIZE bytes)"
            fi
          done
          
      - name: 上传 APK 为 Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ github.run_number }}
          path: build/**/*.apk
          retention-days: 30
          if-no-files-found: warn

      - name: 创建 Release
        if: ${{ github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') }}
        uses: softprops/action-gh-release@v2
        timeout-minutes: 5
        with:
          tag_name: build-${{ github.run_number }}
          name: Build ${{ github.run_number }}
          body: |
            ## 自动构建的 Debug 版本
            
            **构建信息：**
            - Commit: ${{ github.sha }}
            - Branch: ${{ github.ref_name }}
            - Run: #${{ github.run_number }}
            - 平台版本: ${{ env.ACTUAL_PLATFORM_VERSION || 'unknown' }}
            - 编译版本: ${{ env.COMPILE_SDK_VERSION || 'unknown' }}
            - 构建工具版本: ${{ env.ACTUAL_BUILD_TOOLS_VERSION || 'unknown' }}
            
            **APK 说明：**
            此构建包含 Debug 版本的 APK 文件。
          files: build/**/*.apk
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}